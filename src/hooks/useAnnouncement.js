// Re-export the announcement hook from its context source.
// The AnnouncementBanner component is imported directly from components/.
export { useAnnouncementContext as useAnnouncement } from '../context/AnnouncementContext';
