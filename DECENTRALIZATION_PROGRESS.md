# Decentralization Program — Progress Log

Append-only. Format: `- [TASK] status — files — note`

- [META] done — DECENTRALIZATION.md, gh description/topics/homepage — anchor spec + repo meta set
- [F1] done — magnetite-seams/** — 23 tests green
- [Wave1-R/N1+G1] done — backend/magnetite-sdk/{Cargo.toml,src/lib.rs,src/scaling.rs}, magnetite-runtime/{Cargo.toml,src/{lib.rs,capacity.rs,node.rs,shard.rs}}, magnetite-cli/src/main.rs, backend/{Cargo.toml,src/services/{distribution.rs,provisioning.rs}} — Capacity-elastic node: SDK `scaling` feature adds `Shardable` + `ShardScheduler` (Local/Spread) + emergent `MatchConfig::elastic` (player cap DERIVED from `magnetite_seams::discovery::Capacity`, never a constant); runtime `capacity::measure_capacity` (cores via available_parallelism, RAM via /proc + sysctl, emergent shard/player budget), `node::{load_verified_game (BLAKE3 verify, fail-closed), prepare_game/run_node (self-advertise SessionAd via Discovery + serve WasmExecutor::from_bytes by hash)}, shard `HandoffTransport` seam + working `LoopbackTransport` + `NetworkHandoffTransport` fail-closed TODO; single-box multi-shard proven deterministic (`multishard_run_is_deterministic`). CLI gains `magnetite node` (content-address + measure + LAN-advertise + serve); `dev`/`deploy` unchanged. Backend (additive, builds green w/o DB — runtime sqlx queries): `distribution::get_artifact_by_hash` (content-addressed resolve), `provisioning::instance_session_ad` (SessionAd shape, demotes central poll). Tests: seams 23, sdk 234 lib + 93 doctests (scaling), runtime 47 lib + 3, cli 17, backend green + new unit test. Real: everything except cross-node handoff TRANSPORT (seam real, LAN/single-node path real, network transport = documented fail-closed TODO). No money/comms files touched.

- [Wave1-P/P1] done — backend/src/{config.rs,main.rs,services/{payment.rs,marketplace.rs,mod.rs},api/{wallet.rs,webhooks.rs,subscriptions.rs,developer.rs,marketplace.rs}}, backend/src/services/{wise.rs,payout.rs,wallet.rs} DELETED, backend/migrations/20260707_noncustodial_payments.sql, backend/tests/{wallet_tests.rs,service_tests.rs,ax2_tests.rs}, {.env.example,.env.docker,backend/.env.example} — NON-CUSTODIAL crypto payments (§2 + §3.6). RIPPED: Paystack (session/verify/webhook/subscription charging + ZAR→USD `ZAR_USD_RATE`), Wise (client, recipient registration, payout job + `payouts`/`payout_requests` flow), custody (`wallet_balances`/`wallet_transactions`/`developer_balances` reads+writes, deposit/withdraw endpoints, hourly payout batch in main.rs), `PaymentService`/`PayoutService`/`WalletService`/`WiseClient`, config `PAYSTACK_SECRET_KEY`/`WISE_*`/`PAYMENTS_SANDBOX`. REPLACED WITH: `magnetite_seams::payment::PaymentRail` wired at `services::payment::rail()` (default `MockPaymentRail`, `PAYMENT_RAIL=mock`, `protocol_fee_bps` default 0, fully offline); marketplace `purchase_usd` = atomic wallet→wallet `checkout` (developer takes the WHOLE subtotal — 70/30 gone) → verified signed `Receipt` persisted in new `payment_receipts` table and linked to `entitlements.receipt_id`; `verify_entitlement` re-verifies the rail signature so a row alone is never proof; refund = receipt-void + entitlement revoke (nothing to claw back); `api/wallet` is now address-only (`GET /wallet`, `POST /wallet/link`, `GET /wallet/receipts`) plus hosting-fee scaffold (`POST /wallet/hosting/pay` via `open_channel` + `GET /wallet/hosting/:server_id` join-gate); subscriptions = feature flags activated by a non-voided receipt (pay-the-operator via `OPERATOR_WALLET_PUBKEY`), no proration charge, receipt-backed tiers expire instead of auto-renewing. Points/XP untouched (off-chain, not money). Migration is additive: `users.wallet_address`, `payment_receipts`, `hosting_channels`, `entitlements.receipt_id`, fiat tables marked DEPRECATED via COMMENT (no DROP). Build green; `cargo test` 532 passed / 0 failed / 1 ignored, all offline (no DB, no network). REMAINING: real chain rail (TODO(chain) in services/payment.rs — USDC-on-L2 or Solana; `CHAIN_RPC_URL`/`CHAIN_ID`/`STABLECOIN_ADDRESS` are placeholders), off-chain signed channel updates for per-join hosting debits, compensating transfer on refund, signed-challenge proof-of-key on wallet link, and a residual Paystack refund call + custody SQL in `api/admin.rs`/`superadmin/*` (outside P1's file set — needs an owner).
- [Wave1-C/C1] DONE — `backend/src/comms/{mod,bridge,providers,gate,api}.rs`, `backend/migrations/20260708_comms_rooms.sql`, `backend/src/api/{streaming,communities}.rs`, `backend/src/config.rs`, `backend/src/{main,lib}.rs`, `.env.example`, `backend/.env.example` — `CommsProvider` seam wired: `COMMS_PROVIDER` selects builtin|matrix|jitsi|livekit|owncast, all external adapters config-gated and falling back to `builtin` (the DEMOTED in-house chat/voice/streaming stack, kept working, offline, zero external services). Identity bridge mints every join credential from the player's keypair via `AuthProvider::mint_scoped_token`, then renders it to Matrix login token / Jitsi JWT / LiveKit grant. Paid rooms gated on a re-verified `payment_receipts` signature (fails closed). Per-node media: `comms_rooms.media_host` + `streams.media_host` + `voice_rooms.media_host` replace the single global `MEDIA_SERVER_BASE_URL`, which survives only as this node's default. Voice-room join now returns a provider-agnostic credential. 17 new tests; `cargo build` green, `cargo test` 106+123+20+22+43+49+45 pass / 0 fail with no external services. TODOs left in-code: Matrix createRoom + tombstone, LiveKit RoomService pre-create/delete, Owncast per-user chat tokens, drop the derived-account-key fallback once every account carries a pubkey.

- [Wave2-QA] done — `backend/src/{api/{admin,sessions,subscriptions,notifications}.rs, superadmin/{billing,ops,pages,mod}.rs, comms/{mod,gate}.rs, services/{payment,email,distribution,provisioning}.rs}`, `backend/tests/{store_hardening,mx1,ax2}_tests.rs`, `backend/templates/emails/*`, `docker-compose{,.override}.yml`, `deploy/{k8s/*,nomad/backend.nomad}`, `docs/self-hosting/**`, `docs/wise-iban.md` DELETED — **(1) Residual fiat/custody RIPPED** (the files P1 correctly refused to touch): `api/admin.rs` lost the Paystack refund API call + custodial `admin_refund_transaction` (a duplicate of marketplace's correct receipt-void refund), the `payouts/process` + `payouts/:id/cancel` endpoints, `Payout`/`ProcessPayoutRequest`, the `wallet_balances` seed, and `payout_status`; its dashboards now report SETTLEMENT (`total_settled_units`/`voided_receipts`/`settled_receipts` from `payment_receipts`) instead of custodial balances. `api/sessions.rs` lost the `wallet_balances` debit + `wallet_transactions` write + 15/85 `game_revenue` accrual + `PLATFORM_FEE_PERCENTAGE` — a paid session now requires a verified non-voided receipt (fails closed). `superadmin/billing.rs` REWRITTEN to the non-custodial model: the 8 checks are now developer-takes-full-subtotal, protocol-fee-matches-`PROTOCOL_FEE_BPS`, paid-entitlements-are-receipt-backed, receipt-arithmetic-balances, **receipt-signatures-verify (re-verifies stored receipts against the rail in Rust — makes the other checks meaningful)**, voided-receipts-grant-nothing, subscription-charge-matches-tier, and custody-is-dormant (warns if legacy fiat tables hold rows). `superadmin/ops.rs` lost the whole payouts section (no money-moving admin action exists any more); `pages.rs` user view shows linked wallet ADDRESS + receipts instead of balances + payouts. Payout emails/templates deleted; `PayoutComplete` → `PaymentSettled` (legacy `PAYOUT_COMPLETE` string still parsed). `subscriptions.rs` ZAR → USDC (`price_zar` dropped, `prorated_amount_zar` → `prorated_amount_usdc`). Stale tests that MOCKED the deleted model (70/30 split math, custodial refund balance reversal, Paystack/Wise provider routing) replaced with non-custodial equivalents that assert the change. **(2) Identity fallback HARDENED**: new `comms::AccountKey` + `KeyProvenance{Linked,Derived}` makes "named" vs "proven" a TYPE distinction — `for_addressing()` is infallible (routing/display/builtin), `for_authorization()` FAILS CLOSED on derived keys. `join()` demands a proven key for PAID rooms before the receipt lookup; the receipt gate additionally refuses any receipt bound to the account's derived key (forged/back-filled row). Gate's duplicate loader collapsed onto shared `payment::{load_receipt,has_verified_receipt}` (fail-closed, derived-key guard) reused by sessions. Legacy unlinked accounts keep working on free/builtin paths. 5 new guard tests. **(3) Deploy/docs made honest**: Paystack/Wise/`PAYMENTS_SANDBOX`/`WISE_SANDBOX` purged from compose, k8s configmap/secret/deployment and nomad; `PAYMENT_RAIL=mock` + `COMMS_PROVIDER=builtin` + `PROTOCOL_FEE_BPS=0` documented as zero-external-service defaults; **MediaMTX is now OPTIONAL** (compose `media` profile, backend dependency removed, `MEDIA_SERVER_BASE_URL` empty by default); `docs/self-hosting/**` restructured into REQUIRED (Postgres, Redis — stated plainly, not overclaimed) vs OPTIONAL (email/OAuth/media/external comms/chain rail), no-central-cloud framing. **(4) Workspace GREEN**: backend 569 tests, sdk 316 (+327 w/ `--features scaling`), seams 23, runtime 50, cli 17, sandbox 32, anticheat 48 — 0 failures, 0 build errors, all offline. Clippy warnings are pre-existing style lints (backend 50, sdk 11, sandbox 13, runtime 5, cli 3; seams/anticheat 0); every lint introduced by this pass was fixed. NOT DONE: `docs/index.md` still links the deleted `wise-iban.md` (frontend-owned file, needs a one-line removal by that owner); legacy fiat tables retained (marked DEPRECATED, non-destructive) and surfaced by the custody-is-dormant check.

- [Wave2-D/I2] done — `magnetite-seams/src/{discovery.rs,payment.rs,lib.rs}`, `backend/src/api/{discovery.rs,mod.rs}`, `backend/src/main.rs`, `backend/src/services/payment.rs`, `backend/migrations/20260709_discovery_ads.sql`, `magnetite-runtime/src/{tracker.rs,lib.rs}` + `Cargo.toml`, `magnetite-cli/src/main.rs` + `Cargo.toml`, `magnetite-e2e/tests/decentralized_loop.rs` + `Cargo.toml` — **Discovery is real end-to-end.** (1) SEAMS: new `SignedAd` (ad + node key + lease window + Ed25519 sig, canonical field-by-field `signing_bytes`, `MAX_AD_TTL_SECS = 600`) and `SignedWithdraw`, both `verify()`-ing FAIL-CLOSED on forged sig / relabelled key / empty, expired, future-dated or over-long lease; `Filter::accepts` made `pub` so tracker and client apply the identical predicate; new `payment::receipt_admits` = the *pure* half of the paid-access gate (buyer binding + amount cover + rail signature), which `backend::services::payment::has_verified_receipt` now DELEGATES to (DB-only facts — item binding, not-voided, proven-not-derived key — stay where they live), so the backend gate and the offline node path cannot drift. (2) BACKEND TRACKER (a phonebook, not an authority): `POST /api/v1/discovery/announce` (signed, leased, heartbeat-renewed; upsert bound by `WHERE discovery_ads.node_key = EXCLUDED.node_key` so one node can never take over another's `(game,node)` slot — a hijack affects 0 rows and 403s), `GET /api/v1/discovery/sessions` (game/max_ping/free_slots_only/free_only/max_price/limit → seam `Filter`; only unexpired leases ever served), `DELETE /api/v1/discovery/announce` (signed deregister). Serializes the EXACT seam shape via `#[serde(flatten)]` — `{game, node, capacity{cpu_cores,ram_mb,bandwidth_mbps,free_slots,max_shards}, ping_hint, price{amount,currency,unit}|null, chat_room, voice_room}` + tracker bookkeeping `{id, node_key, players, max_players, expires_at}`, under the standard `{success,data:{sessions,total}}` envelope the frontend's `useDiscovery` already parses. `game` is plain BLAKE3 hex (frontend mocks show a cosmetic `b3:` prefix; equality-filtering is unaffected and plain hex round-trips through `Hash`). Restart behaviour is honest: `discovery_ads` persists rows so a restart doesn't blank the phonebook, but nothing there is authoritative — every lease lapses within 10 min unless the node heartbeats, so even a restored-from-backup tracker converges to whoever is actually up. (3) RUNTIME: `tracker::HttpTrackerClient` implements the seam's `TrackerClient` over reqwest, SIGNING every announce with the node's key, clamping TTL to the protocol max, tolerating all three response envelopes and skipping (never guessing at) malformed ads; `tracker::from_env` returns `None` unless `TRACKER_URL` is set — `LanDiscovery` stays the zero-config default and NO external service is required. `magnetite node` opts in when `TRACKER_URL` is set, with a stable node key (`MAGNETITE_NODE_SEED` or derived from bind addr; TODO(node-key) persist a real keypair) and treats an unreachable tracker as a lost hint, not a failed boot. (4) OFFLINE E2E PROOF (`magnetite-e2e/tests/decentralized_loop.rs`, 4 tests, no chain / no Matrix / no live tracker / no Postgres / no socket): content-addressed load + BLAKE3 verify (rejects a lying store and a missing blob), capacity measured → emergent player cap → self-announce, client `find` sees the right hash/capacity/price, mock checkout → signed receipt (tampered amount AND redirected payee both rejected), builtin comms credential minted from the player's own key, paid-room admission through the SHARED `receipt_admits` gate with all four fail-closed paths asserted (forged / another player's / underpaid / genuine-but-for-a-cheaper-item), and deterministic multi-shard ticking on one box (plus the anti-vacuity check that different inputs give a different world). Also asserts a forged announce, a validly-signed slot hijack, a lapsed lease, a forged withdrawal, an unsigned ad, an over-long lease and a backdated ad all fail to reach the phonebook. TESTS: backend 587 (was 569), seams 29 (was 23), runtime 54 lib + 4 doc (was 50), cli 17, sdk 316/327, sandbox 32, anticheat 48, e2e +4 new. `magnetite-e2e/tests/wasm_end_to_end.rs` 2 failures are PRE-EXISTING and environmental (needs `cargo build --release --target wasm32-wasip1 --features wasm` in game-template-authoritative first). Clippy: 0 new warnings (backend still 50 pre-existing, 0 in the new files; seams 0). REMAINING: no live HTTP round-trip test against the real axum router (the e2e proof drives the same envelope + parser in-process to stay socket-free); tracker heartbeat is caller-driven rather than a background task in `run_node`; `MAGNETITE_NODE_SEED` derivation is a stopgap for a persisted node keypair; no multi-tracker fan-out/gossip yet (a client queries one tracker at a time).

- [Wave2-DI] done — `magnetite-seams/src/{discovery.rs,lib.rs}`, `backend/src/api/discovery.rs`, `backend/src/services/provisioning.rs`, `backend/migrations/20260719_discovery_node_labels.sql`, `backend/tests/discovery_http_tests.rs`, `magnetite-runtime/src/{node.rs,tracker.rs,lib.rs}`, `magnetite-cli/src/main.rs`, `magnetite-e2e/tests/decentralized_loop.rs`, `src/hooks/useDiscovery.js`, `src/pages/{ServerBrowser.jsx,ServerBrowser.css}`, `src/pages/__tests__/ServerBrowser.test.jsx`, `docs|site/screenshots/**` — **The server browser is wired to reality.** (1) SHAPE RECONCILED, both sides, with provenance made explicit rather than blurred. The frontend mocked `game_title`/`node_operator`/`region`/`version` and `b3:`-prefixed hashes; none of those existed on the wire, so live data rendered `undefined`. Resolution, field by field: **game title/version are TRACKER-RESOLVED** — the backend now joins `game_artifacts`→`games`/`game_versions` on the announced content hash (`DISTINCT ON`, newest successful artifact) and returns `game_title`/`game_version`, which are **null for any hash this tracker has never indexed** (the normal case in a decentralized network, not an error); the lookup **fails soft** (a catalog query error logs and serves hashes, never fails the phonebook) because the authoritative half of the response is the signed ads. **Operator/region are NODE-DECLARED** — a tracker cannot verify where a box is or who runs it, so they became `Option<String>` fields *inside* the signed `SessionAd` body (covered by `signing_bytes`, so a relay cannot relabel someone else's box) and are rendered with a `self-declared` chip and a "signed by its key, but not verified by any tracker" tooltip. A phonebook reports what nodes claim; it does not certify it. **Hash form is plain hex everywhere** — `b3:` is gone from the frontend's data expectations; the `BLAKE3` chip is a rendering choice. The mocks are now a faithful mirror of the real payload *including its nulls*, with two rows deliberately exercising the degraded paths (uncatalogued hash; node that declared no operator/region and publishes no occupancy counters). Every nullable field degrades: unknown title falls back to the short content address (rendered ONCE, as the title, not duplicated as title+chip), missing version → "not in this tracker's catalog", no labels → "no operator declared", absent `players`/`max_players` → "not reported" while the *signed* `free_slots` is still shown. Also made the network summary honest: "independent operators" counted self-declared names, so it now counts **distinct `node_key`s** — the one thing on that row that is actually proven. (2) TRACKER HEARTBEAT (the functional gap): `run_node` had no renew task, so a long-lived node silently dropped out of the phonebook when its lease expired. Added `node::{Heartbeat, spawn_heartbeat, DEFAULT_LEASE_SECS}` — renews every `lease/2` with ±10% jitter (many nodes booted by one orchestrator would otherwise hit a tracker in lockstep), retries failures with exponential backoff from 1s **capped at the renew interval** (never back off past it — the lease is ticking), and `run_node` now stops the heartbeat and **retracts the ad on exit, clean or not**. Required a `Discovery::withdraw` default method (no-op default; `LanDiscovery` removes the slot, `TrackerDiscovery` delegates to a new `TrackerClient::withdraw` that the HTTP client wires to its existing *signed* `SignedWithdraw` call — previously that method existed but nothing reached it). Added `FanoutDiscovery` (announce succeeds if ANY backend accepted — one dead tracker must never stop a node hosting; `find` merges and de-dups on `(game,node)`) so the CLI's LAN ad **and** its `TRACKER_URL` ad are announced, renewed and retracted through one handle; previously the CLI announced to the tracker once, out-of-band, and never renewed it. **Two real bugs found and fixed while testing this:** a lost-wakeup race (`notify_waiters` drops the stop signal if the task is not yet parked, so shutdown blocked for a whole interval — the node-tests suite took 319s; `notify_one` is permit-based and fixed it, suite now 1.06s), and a 2s minimum-lease clamp that silently overrode short leases. (3) LIVE HTTP TEST (the flagged gap): `backend/tests/discovery_http_tests.rs` binds a **real TCP socket on an ephemeral port**, serves the real axum router, and speaks HTTP with reqwest — 8 tests. The pool is `connect_lazy`, so **no Postgres and nothing external is required**. Being honest about the split: every *rejection* path is fully proven over the wire (unsigned, price-tampered, key-relabelled, operator-relabelled, lapsed lease, over-long lease, garbage bodies, non-hex game filter, forged withdrawal) because verification runs entirely before any query — that is the security-relevant half and the half a fail-open would live in, and the tests assert those return 4xx *without* reaching the DB. The *success* path is proven only as far as reachability (routes mounted at the paths the node client posts to; the flattened wire form and the browser's own filter query both parse; an honest announce survives every check and reaches the storage layer). A stored-and-served round trip genuinely needs Postgres; faking it with an in-memory store would test a fake rather than the router, so that is left to an integration environment. (4) Screenshots re-run: 24 captured, 0 failed. TESTS: seams 35 (was 29), runtime 58 lib + 4 doc (was 54), backend 603 (was 587; +8 live-HTTP, +4 shape/degradation units), cli 17, sdk 316, sandbox 32, anticheat 48, e2e decentralized_loop 4, frontend vitest 535 (was 526; +9 ServerBrowser contract tests asserting no `undefined`/`NaN`/`null` renders for any row shape). 0 failures anywhere; eslint clean; 0 new clippy warnings (runtime's 5 are pre-existing in server/tick/shard). No fail-closed behaviour weakened: signatures, slot ownership, receipt verification and `KeyProvenance::for_authorization` untouched. REMAINING: stored-and-served round trip needs a DB-backed integration env; `MAGNETITE_NODE_SEED` is still a stopgap for a persisted node keypair; `NodeConfig.operator`/`region` are plumbed but the CLI has no flags to set them yet; no multi-tracker gossip (fanout is static config, not discovery-of-trackers).

- [Wave3-I1] DONE — `magnetite-seams/Cargo.toml` (new `dmtap` feature, default off, **zero** new deps), `magnetite-seams/src/keyname/mod.rs` (new), `magnetite-seams/src/keyname/wordlist.rs` (new), `magnetite-seams/src/lib.rs` (feature-gated module + re-export + doc note), `magnetite-seams/tests/seam_pluggability.rs` (new), `docs/dmtap.md` (new), `docs/index.md`, `site/docs.html`, `site/docs/dmtap.md` (mirror) — **The gap this closes:** Comms (5 impls), Discovery (3) and BlobStore (2) already proved their seams pluggable, but Naming and Identity each had exactly one implementation, so nothing showed they weren't accidentally hardwired to their default. Added `KeyNameNaming`, a second real `Naming` provider behind `--features dmtap` (OFF by default): word-based, zero-authority key-names over an embedded 2048-word list (11 bits/word), plus optional `<words>@domain` display from a *local, non-authoritative* domain hint. **Honest arithmetic, stated plainly rather than papered over:** 8 words carry 88 bits and an Ed25519 key is 256, so no 8-word scheme — not ours, not DMTAP's — can losslessly encode a key. So two forms ship: `short_name` = 8 words = 88-bit domain-separated BLAKE3 fingerprint (the display name; resolves only for keys the node has *learned* out of band), and `full_name` = 24 words = 256 key bits + 8 checksum bits = 264 = 24x11 exactly (a pure encoding, exact round-trip for every key, BIP-39-*shaped* but not BIP-39). Wordlist derived mechanically from `/usr/share/dict/words` (Webster's Second "web2", **public domain**; no BIP-39 list vendored, so no Bitcoin-project licence question): keep `^[a-z]{4,7}$`, keep only the first word per 4-char prefix (every word unambiguous from its opening), sort/dedupe to 15491, take 2048 evenly spaced so names span the alphabet — and the list's invariants (2048, sorted, unique, unique 4-prefixes, ASCII) are asserted by tests so a corrupted list fails CI instead of silently renaming every key. Fail-closed throughout: unknown word, wrong word count, bad checksum, second `@`, or a domain that disagrees with the learned hint all return `None`/`Err`; a fuzz-ish junk battery (empty, control chars, astral chars, 200-word input) asserts no panic. **NOT NAMED `DmtapNaming`, deliberately:** `dmtap-core` is not on this machine and is not published, so its wordlist/packing/checksum/separator could not be read and compatibility could not be verified — names here will almost certainly NOT match DMTAP's for the same key, and putting an unverifiable compatibility claim in a *type name* is the worst place to put one. `docs/dmtap.md` says this in as many words and enumerates what awaits the real crate (`DmtapAuth`, wire-compatible names, `DmtapPubBlobStore`/MOTE, a DMTAP comms provider, and any DMTAP dependency at all — the `dmtap` feature adds none). Pluggability proof (`tests/seam_pluggability.rs`, 4 tests): one unmodified consumer written only against `dyn Naming` is driven by both `HashNaming` and `KeyNameNaming` with identical assertions, a `Box<dyn Naming>` provider-set swaps implementations with no consumer edit, and both keep raw hex resolvable so the substrate stays reachable. For Identity/Auth a *test-double* `CounterAuth` (lives only in the test file, clearly labelled) proves the seam and the generic `BuiltinProvider<A: AuthProvider>` accept a foreign impl — no second *shipped* auth provider was invented, because none is honest without an external service or the missing crate. TESTS: seams **35 (feature off, unchanged)** / **52 (feature on: 48 lib + 4 integration)**; runtime 58 + 4 doc, backend 603, cli 17, sdk 316, sandbox 32, anticheat 48, frontend vitest 535 — all unchanged, 0 failures; clippy clean with the feature on (`large_const_arrays` fixed by making the wordlist a `static`). Default build is byte-identical: no new dependencies, no new default module. REMAINING: the actual `dmtap-core` integration is blocked on the crate existing/being reachable; short-name resolution needs a real key-learning path wired from signed discovery ads (today `learn()` is called by whoever holds the key).
