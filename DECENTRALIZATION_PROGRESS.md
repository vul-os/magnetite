# Decentralization Program — Progress Log

Append-only. Format: `- [TASK] status — files — note`

- [META] done — DECENTRALIZATION.md, gh description/topics/homepage — anchor spec + repo meta set
- [F1] done — magnetite-seams/** — 23 tests green
- [Wave1-R/N1+G1] done — backend/magnetite-sdk/{Cargo.toml,src/lib.rs,src/scaling.rs}, magnetite-runtime/{Cargo.toml,src/{lib.rs,capacity.rs,node.rs,shard.rs}}, magnetite-cli/src/main.rs, backend/{Cargo.toml,src/services/{distribution.rs,provisioning.rs}} — Capacity-elastic node: SDK `scaling` feature adds `Shardable` + `ShardScheduler` (Local/Spread) + emergent `MatchConfig::elastic` (player cap DERIVED from `magnetite_seams::discovery::Capacity`, never a constant); runtime `capacity::measure_capacity` (cores via available_parallelism, RAM via /proc + sysctl, emergent shard/player budget), `node::{load_verified_game (BLAKE3 verify, fail-closed), prepare_game/run_node (self-advertise SessionAd via Discovery + serve WasmExecutor::from_bytes by hash)}, shard `HandoffTransport` seam + working `LoopbackTransport` + `NetworkHandoffTransport` fail-closed TODO; single-box multi-shard proven deterministic (`multishard_run_is_deterministic`). CLI gains `magnetite node` (content-address + measure + LAN-advertise + serve); `dev`/`deploy` unchanged. Backend (additive, builds green w/o DB — runtime sqlx queries): `distribution::get_artifact_by_hash` (content-addressed resolve), `provisioning::instance_session_ad` (SessionAd shape, demotes central poll). Tests: seams 23, sdk 234 lib + 93 doctests (scaling), runtime 47 lib + 3, cli 17, backend green + new unit test. Real: everything except cross-node handoff TRANSPORT (seam real, LAN/single-node path real, network transport = documented fail-closed TODO). No money/comms files touched.
